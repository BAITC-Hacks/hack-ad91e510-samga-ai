# Прогресс

## Версия 2 — в работе
После обратной связи Карима переделываем сценарий в сравнение трёх планов и их последствий. Все виджеты первой версии заменяются. Проанализированы capital/source/cheap. Фокус на компьютере. Следующие шаги: модуль сравнения и новый экран.

Вторая версия реализована: отдельная карта, светлая область объяснений, три постоянно видимых плана; показатели района до/после, вывод для акима, раскрываемые проекты с изменением районов, расчёт, 5 этапов автопоказа. Старые demo-state и его тест удалены как больше не используемые. comparison.mjs и 7 тестов созданы через RED→GREEN.

Браузерная проверка: capital сохраняет школы38/медицину35; source даёт48/43.75 при том же бюджете95; cheap показывает медицину37.63 и отрицательный T1=53.25. Перенос одной школы capital→Нура сохраняет95, даёт schools48 и critical1, Score55.1 на экране. Before/after проверен. Все5 маркеров absolute и находятся внутри карты; scrollWidth=innerWidth=1280.

Исправлены реальные замечания: invalid не подставляет baseline Score; при обновлении меток сохраняется системный класс MapLibre через classList.toggle (раньше className удалял absolute-позиционирование). Обзор подгоняется под пять районов. Повторное независимое ревью замечаний не выявило.

Финал V2: 19/19 тестов (12 исходной модели + 7 сравнений), app/map синтаксически корректны, diff --check чист. Исходный astana-city и model/data не изменены. Авторазбор остановился на 5/5 с текстом «Разбор завершён»; консоль без ошибок/предупреждений. Свой вариант: школа→Нура показала55.1/crit1, затем поликлиника→Нура56.3/crit0; восстановление вернуло95/crit2. Новая закреплённая строка редактора показывает результат во время изменения. Пауза и before/after проверены. Версия для компьютера готова; публикация не выполнялась.

23.09.2026: изучены исходный макет и модель, выбран отдельный demos/astana-story. Создана ветка karim/simulator. Следующее: последовательность демо, интерфейс и интеграция карты.

Реализованы отдельная страница, карта с условными метками, пять решений, бюджет, 8 кварталов, финал, сравнение и управление показом. Модуль состояния создан тест-сначала (RED: отсутствующий модуль, GREEN: 6 тестов). Набор с исходной моделью: 18 проверок.

Браузер: вручную пройдены все 15 переходов; бюджеты 18/33/57/69/85, финальный индекс 55,7, критических значений 1; промежуточный индекс сохранён исходным. Сравнение вернуло 52,6 и затем 55,7; повтор вернул начало. Исправлена вложенность HTML пояснений, мешавшая вёрстке; панели переведены в grid, чтобы нижняя полоса не перекрывала их. Обнаружен отсутствующий wood-pattern в картографическом стиле: в отдельной демоверсии слой использует сплошной цвет.

Уточнение Карима: мобильную версию пока не делать. Дополнительная мобильная работа остановлена, временное изменение размера браузера сброшено; завершаем версию для компьютера. Независимое read-only ревью логики демо не выявило проблем. Пауза проверена: после остановки в начале бюджет оставался 0. Автопоказ доходит до фазы кварталов; финальная автоматическая остановка проверяется отдельно.

Финальная проверка завершена: в отдельной вкладке автопоказ дошёл до phase-result, текста «Сценарий завершён», кнопки «Повторить демо», индекса 55,7. Консоль отдельной вкладки: 0 errors/warnings. Все 18 node:test прошли, синтаксис app/map прошёл. `git diff --exit-code HEAD -- demos/astana-city docs/brief-analysis/dist/model.mjs docs/brief-analysis/dist/data.mjs` вернул 0: исходный макет и расчётные данные сохранены. Изменения находятся только в demos/astana-story; внешний push не выполняется.
