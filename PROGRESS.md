# Журнал фактического прогресса

Дата соревнования: 23.09.2026. Часовой пояс: Asia/Almaty (UTC+5).
Записываем реальный результат и его проверку, не меняем даты коммитов задним числом.
Локальный коммит и доставка в GitHub — отдельные события. Принятие часового результата
организатором этим журналом не подтверждается; отдельный способ отчётности пока не выяснен.

## Этап 1 — разбор задания и проверяемая модель

Материалы подготовлены в первый конкурсный час 13:00–14:00. В 13:47 при проверке
GitHub в основном репозитории был только `4268c27` от 20.09.2026 (`Initial commit`).
По поручению Карима результат переносится из рабочей тетради в официальный репозиторий.
Фактическое время сохранения этого этапа показывает `git log`, время доставки — GitHub.

**Сделано:**

- Сохранены оригиналы ТЗ и синтетического датасета, подготовлены читаемые извлечения.
- Разобраны обязательные требования, критерии оценки и неоднозначности задания.
- Подготовлены данные: 5 районов, 10 показателей, 14 мер.
- Реализован расчёт с лагами, синергиями, весами населения, слабейшим районом и штрафами.
- Подготовлены 4 сценария сравнения и самостоятельный локальный аналитический сайт.
- Добавлены 12 автоматических проверок модели и инструкции воспроизведения.

**Участие:** работу в этой локальной копии ведёт Карим (`kmakhutov93-ai`) с помощью
Codex; конкретный вклад других участников в этот этап не приписывается без подтверждения.

**Проверено перед коммитом:**

- На Node.js 24.13.0 прошли 12/12 тестов; синтаксис трёх ES-модулей проверен.
- 14 перенесённых файлов (код, тесты, данные, иллюстрации, оригиналы, извлечения
  и промпты) побайтно совпадают с рабочей тетрадью; README адаптирован к новому пути.
- Проверены локальные ссылки HTML и Markdown.
- Сайт запущен командой из README на Python 3.14.7, `127.0.0.1:4193`.
  В браузере пример датасета показывает 56,54, после выбора «Те же меры — в Есиле» —
  53,86 при стоимости 95; критических значений соответственно 0 и 2. Ошибок
  в журнале браузера при этой проверке нет.

Команды и точные эталонные значения приведены в [README](README.md).

**Остаётся:** пользовательский выбор пяти решений, живой AI-анализ,
полный путь симулятора, уточнение противоречия PDF/DOCX о направлениях.

## Объединение работы перед отправкой первого этапа

Во время push в `main` появился коммит Аслана (`gtaslan2201`) `b5738e7`
с описанием проекта, пользовательским сценарием и требованиями MVP.
Его история сохранена; README объединён с проверенной моделью и инструкциями запуска.
Вопросы о бюджете и формуле уточнены по уже полученному DOCX. Исходный коммит
аналитического этапа `4379eee` также сохраняется в истории без переписывания дат.

## Этап 2 — выбранный макет для совместной доработки

23.09.2026 Карим выбрал ASTANA / City Lab как визуальную основу продукта.
Макет перенесён из локальной заготовки в `demos/astana-city/` для передачи
команде через этот репозиторий. Работу по переносу и документации выполнил
Карим с помощью Codex; вклад других участников здесь не приписывается.

**Сделано:** перенесены интерфейс, локальная MapLibre с лицензией, стиль,
шесть контуров районов и три изображения экранов. Данные и формула
используются из уже имеющегося `docs/brief-analysis/dist/` без дублирования.
В коде макета изменены только три пути. Добавлены `AI_START.md`, инструкция
запуска и описание происхождения компонентов. Командный коммит `4c42641`
получен перед переносом и сохранён в истории.

**Проверено перед коммитом:**

- 12/12 тестов расчётной модели; `node --check demos/astana-city/app.mjs`.
- 10 файлов побайтно совпадают с источниками; `app.mjs` отличается только
  путями импортов данных, модели и GeoJSON. JSON корректен; 24 локальные
  ссылки в README, AI_START и SOURCES ведут к существующим файлам.
- Отдельный сервер запущен из корня репозитория на `127.0.0.1:4196`
  (4195 занят исходным макетом). В Chrome проверены здания, переход
  52,6 → 55,7 и обратно, выбор Нуры, слой границ районов и отсутствие
  показателей у Сарайшыка. Изображения экранов в пакете сохранены из исходной заготовки.
- В журнале браузера нет ошибок уровня error. Есть предупреждение MapLibre
  об отсутствующем изображении `wood-pattern` во внешнем sprite; оно не
  помешало проверенным сценариям и сохранено как ограничение текущего макета.

**Остаётся:** самостоятельный выбор плана, живой AI-анализ и полный сценарий
продукта. Этот этап фиксирует основу для совместной работы, а не готовность
конкурсной версии или её принятие организатором. Прямой push в `main` заблокирован
локальным защитным хуком. Для передачи выбрана ветка `demo/astana-city-base`;
доставку подтверждает наличие коммита в этой удалённой ветке, а не эта запись сама по себе.

## Пакет для просмотра и локального запуска командой

23.09.2026 по поручению Карима добавлены `START-HERE.md`, файлы запуска
для Windows/macOS и `start-demo.py` без внешних пакетов. Сервер выбирает
свободный порт на `127.0.0.1`, открывает браузер и находит файлы независимо
от текущей папки. MIME-тип `.mjs` задан явно для запуска ES-модулей на Windows.
Макет и расчётная модель не изменены.

Работа выполнена в отдельной копии `demo/astana-city-base`; параллельная
ветка `karim/simulator` и её незакоммиченные файлы не затронуты.

Проверка: интеграционный тест запуска из другой папки сначала упал из-за
отсутствия запускатора, после реализации прошёл. Он проверяет страницу,
импорты модели/датасета, JavaScript MIME и GeoJSON реальными HTTP-запросами.
Синтаксис Python и shell проверен. 12 тестов исходной модели прошли в отдельной
копии перед работой. Windows-файл проверен по коду; нативный запуск на Windows
в этой macOS-сессии не проверялся.

Для передачи выбран приватный GitHub-релиз `astana-city-demo-v1`
с ZIP всего снимка проекта. Факт загрузки подтверждается наличием артефакта
в релизе. Публичный доступ к сайту и репозиторию этим этапом не включается.

## Как записывать следующие этапы

По завершении каждого конкурсного часа (14:00, 15:00, 16:00, 17:00, 18:00,
если организатор не изменит расписание) добавлять: интервал, фактический результат,
участников, файлы/commit, выполненную проверку и оставшиеся ограничения.
Пустой коммит не заменяет результат. Эти часы — следствие объявленного старта в 13:00.

Правила сверены 23.09.2026 по действующим §§5.3.1, 5.4.4–5.4.16
[официального положения](https://edu.astanahub.com/hackathons/df4743f5-c492-415c-b45a-1f13adb78e06?tab=regulations).

## Backend/API — работа Меру в meru/ai

- Добавлен Node.js backend без внешних зависимостей: simulate, analyze, health, contracts.
- Создан контракт v1 между frontend, engine и AI. Simulator engine и его формула не менялись.
- OpenAI Responses API получает готовые числа; текст проверяется по строгой схеме.
- Добавлены явный demo-режим, ошибки конфигурации/провайдера/таймаута,
  ограничение тела и параллельных запросов, CORS для локального frontend.
- Добавлены README backend и воспроизводимый демонстрационный клиент.
- Проверено: 14 новых backend-тестов и 12 существующих тестов модели, всего 26/26.
- Вклад: backend-работа Меру с помощью Codex; вклад в engine Карима не переатрибутируется.
- Ограничения: frontend ещё не подключён; живой OpenAI-вызов не проверен,
  ключ и модель в окружении не заданы. API предназначен для локального демо.

## Текущий этап — тестирование макетов и сценариев, доработка каркаса

23.09.2026. Работа продолжается в ветке `feat/verified-analytics`.

- Сейчас тестируем макеты и пользовательские сценарии, дорабатываем каркас приложения.
- В рабочей версии доступны выбор пяти мер, серверный расчёт, поиск стратегий,
  сравнение планов и объяснения на основе проверенных фактов.
- В коммите `74260c4` упрощён интерфейс плана и добавлен предпросмотр замены мер перед применением.
- При текущей проверке прошли 46/46 автоматических тестов модели, backend и аналитики:
  `node --test --test-isolation=none docs/brief-analysis/test/model.test.mjs backend/test/api.test.mjs analytics/test/*.test.mjs`.
  Флаг отключения изоляции использован из-за ограничения среды на запуск дочерних процессов.
- Этап не завершён: остаются доработка каркаса, объединение командного интерфейса
  с backend и проверка полного пользовательского пути с живым AI.
- Запись статуса подготовлена Меру с помощью Codex. Автоматические тесты используют
  заглушки AI; визуальное тестирование макетов продолжается.

## 23.09.2026 — карта и проверяемая аналитика объединены

- Ветка feat/city-simulator-integration, основа Карима demo/astana-city-base.
- Пять редактируемых проектов вместо фиксированных карточек: атомарная замена, район, предпросмотр, ограничения.
- Карта, бюджет, состояние через два года и районные показатели обновляются вместе.
- Сохранение/восстановление гипотезы, сравнение, полный поиск по четырём целям.
- Десять показателей района, объяснение серверных фактов, мировой опыт, явный статус неподключённого AI.
- Без новых runtime-зависимостей. Прежний аналитический экран по /analytics/.
- 46 тестов модели/API/аналитики прошли; браузер проверил замену, отмену, конфликты, бюджет, сравнение, план за 66, факты, источники и мобильное редактирование.
- Реальные тайлы и 3D проверены в Edge на 1440×950 и 390×844. Источники карты и ограничения сохранены.

- Следующая UX-доработка: первый шаг «На что потратим 100 единиц?», крупный остаток бюджета, пример организаторов 95/100 с объяснением плюса и минуса, явная кнопка «Посмотреть будущее».
