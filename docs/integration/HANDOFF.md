# Локальный результат и следующий шаг

Основная ветка: `karim/integration-20260923`. Рабочая копия:
`/Users/karim/Desktop/HackatonDay/samga-integration`.
Оригинальный checkout `samga-ai` и параллельная работа не переключались.

## Подтверждено

- 77/77 Node тестов: модель, API, факты, поиск, сессия карты, импорт и briefing.
- 2/2 Python теста запускатора; shell и JavaScript syntax, git diff --check.
- Настоящий браузер: пустой план, ручной выбор, блокировки, серверный расчёт,
  сравнение, переход карта→briefing→карта и явный demo AI.
- DOCX пример: 52.55768→56.54307, расход95, критические2→0.
- HTTP аудит жюри: 14/14, `requiredAiVerified:false`.
- Исходные документы в репозитории побайтно совпадают с файлами организатора.

## Не подтверждено

- Живой OpenAI. Нужна локальная конфигурация `backend/.env` с командным ключом
  и доступной моделью; значения не передавать в чат. После настройки:
  `node --env-file=backend/.env backend/server.mjs`, затем один живой прогон
  `node scripts/jury-check.mjs --url http://127.0.0.1:4197 --live-ai` и браузер.
- Нативный запуск Windows и запуск на другом ноутбуке. BAT проверен чтением;
  macOS процессы, HTTP и чистая архивная копия проверяются отдельно в progress.md.
- Получение ответа организатора о пяти направлениях, отправка в GitHub и сдача.
- Исторический Playwright browser script Аслана не запускался: необязательный
  пакет в этой копии не установлен. Основной UI проверен настоящим браузером Codex.

## После прямого разрешения Карима

Проверить текущий аккаунт (`gh api user --jq .login` должен вернуть
`kmakhutov93-ai`), обновить remote refs, убедиться в чистом diff и точном SHA.
Отправить только интеграционную ветку:

```sh
git push -u origin karim/integration-20260923
```

Затем открыть PR в `BAITC-Hacks/hack-ad91e510-samga-ai` против main с описанием
проверок и явного AI ограничения. Это не разрешение на merge или публикацию сайта.
Удалённый SHA и принятую конкурсную ветку нужно подтвердить до18:00 Алматы.

PR #1 исправлен отдельно: `/Users/karim/Desktop/HackatonDay/samga-pr1-fix`,
ветка `karim/pr1-ignore-fix`, SHA `4b11646`. Правила обеих сторон сохранены,
diff против main — только22 строки `.gitignore`. После отдельного разрешения
эту ветку можно отправить в `chore/exclude-secrets` без force-push:

```sh
git push origin karim/pr1-ignore-fix:chore/exclude-secrets
```

Вопрос организатору (самостоятельно не отправлялся): «Верно ли, что действуют
правила DOCX: пять разных мер, максимум две одного направления, и охват всех
пяти направлений не обязателен?»
